# CSV Statistics CLI Tool

A simple Python CLI tool that reads CSV files and outputs summary statistics.

## Features

- Row count
- Column means
- Min/max values per column
- Clean, formatted output

## Installation

